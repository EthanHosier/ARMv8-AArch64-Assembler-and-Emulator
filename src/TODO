wide_moves:
    movn
    movz

shifts:
    asr1
    asr2
    ror1
    ror2

generated:
    145 other incorrects

flag for adds incorrect (e.g. cmn15)
flags for subs incorrect (e.g. cmp17)
branching incorrect (also check movk) (e.g. br13, br6)